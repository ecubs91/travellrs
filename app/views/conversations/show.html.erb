<div class="container">
  <div class="row">
    <div class="col-md-7">
      <h2>Conversation with <%= @conversation.participants.map{|a| !(a.id==current_user.id) ? "#{a.first_name} #{a.last_name}" : nil }.compact!.first %></h2>
      <br>
<%= render 'reply_message_form' %>
     
<%= render 'messages_from_conversations_receipt' %>

      <%= link_to 'Back to conversations', conversations_path, class: "btn btn-primary" %>
    </div>
    <div class="col-md-4">
      
    </div>
  </div>
    
